{
    "version": "0.2.0",
    "configurations": [
        {
            "cwd": "${workspaceFolder}",
            "name": "download_file",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/download-file",
            "args": [
                "-H", "localhost",
                "-p", "9000",
                "-d", "${workspaceFolder}/lib/files-client/cl2.mp4",
                "-n", "server2.mp4",
                "-v"
            ]
        }
    ]
}