{
    "version": "0.2.0",
    "configurations": [
        {
            "cwd": "${workspaceFolder}",
            "name": "upload_file",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/upload-file",
            "args": [
                "-H", "localhost",
                "-p", "9000",
                "-d", "${workspaceFolder}/lib/files-client/test2.mp4",
                "-n", "server2.mp4 ",
                "-v"
            ]
        }
    ]
}